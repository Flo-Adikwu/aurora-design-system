export { Modal } from './Modal';
export type { ModalProps } from './Modal';


// git commit -m "feat: Add Modal component with focus trap and accessibility

// - Implement Modal dialog with size variants (sm, md, lg, xl, full)
// - Add focus trap and keyboard navigation
// - Include body scroll lock when modal is open
// - Support ESC key and overlay click to close (configurable)
// - Add header with optional title and close button
// - Include optional footer for actions
// - Add comprehensive test coverage (50+ test cases)
// - Create Storybook documentation with 12+ examples
// - Ensure full accessibility with ARIA roles and focus management"